import 'package:flutter/material.dart';

class AppColors {
  AppColors._();
  
  // Custom Steel Blue & Orange Palette
  static const primary = Color(0xFF5E6D85);              // Steel Blue - Cards, default surfaces
  static const primaryDark = Color(0xFF4E5F78);          // Darker Steel Blue - Pressed/focused states
  static const accent = Color(0xFFFF7A45);               // Orange - CTAs, highlights
  static const accentLight = Color(0xFFFFB74D);          // Light Orange - Sub-menu, secondary accent
  static const background = Color(0xFF616F88);           // Background - Scaffold
  static const card = Color(0xFF5E6D85);                 // Card surfaces
  static const header = Color(0xFF40536C);               // Steel Blue - App bar, menu
  static const textPrimary = Color(0xFFFFFFFF);          // White - Primary text
  static const textSecondary = Color(0xFFD6DCE6);        // Light gray - Secondary/muted text
  static const divider = Color(0xFFE6EAF0);              // Dividers and borders
  static const brandOrange = Color(0xFFFFA726);          // Brand/theme color, sub-menu
  static const golden = Color(0xFFFFD54F);               // Golden - Text accents
  
  // Utility colors (non-theme specific)
  static const cameraBackground = Color(0xFF828282);
  static const iconSecondary = Color(0xC2000000);
  static const textWhite = Color(0xFFFFFFFF);
  static const textTertiary = Color(0x8A000000);
  static const black = Color(0xFF000000);
}
